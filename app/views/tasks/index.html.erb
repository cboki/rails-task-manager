<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= form_for task do |f| %>
    <%= f.check_box :completed %>
    <%= link_to task.title, task_path(task) %>
    <%= link_to 'Delete', task_destroy_path(task), method: :delete, data: { confirm: 'Are you sure ?'} %>
    <% end %>
  </li>
  <% end %>
</ul>
<%= link_to 'Add a new task', tasks_new_path %>
